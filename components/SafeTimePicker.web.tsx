import React from 'react';

// On Web, we handle the time picker using the native HTML input in the settings row.
// This component is a placeholder to prevent the Native DateTimePicker package from being imported/bundled.
export function SafeTimePicker(props: any) {
    return null;
}
